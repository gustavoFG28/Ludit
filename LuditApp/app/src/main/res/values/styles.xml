<resources>

    <!-- Base application theme. -->
    <style name="FullScreen" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:fontFamily">@font/fredoka</item>
        <item name="android:editTextStyle">
            @style/App_EditTextStyle
        </item>
        <item name="android:buttonStyle">@style/App_Button</item>
    </style>

    <style name="DataPickerTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">@color/startColorGradient</item>
        <item name="colorControlActivated">#000</item>
        <item name="android:selectableItemBackgroundBorderless">@color/endColorGradient</item>
        <item name="colorControlHighlight">@android:color/holo_green_dark</item>
    </style>

    <style name="App_EditTextStyle" parent="@android:style/Widget.EditText">
        <item name="android:textColor">#808080</item>
        <item name="android:textSize">24dp</item>

    </style>

    <style name="App_Button" parent="@android:style/Widget.Button">
        <item name="android:background">@drawable/button_gradient</item>
        <item name="android:color">#ffffff</item>
    </style>

    <style name="DialogFoto">
            <item name="android:background">#fff</item>
    </style>
</resources>
